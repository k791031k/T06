javascript:(async function(d){var id='kqt-script';if(d.getElementById(id))return;try{const jsUrl='https://cdn.jsdelivr.net/gh/k791031k/-/main-app.js?v='+Date.now();const cssUrl='https://cdn.jsdelivr.net/gh/k791031k/-/styles.css?v='+Date.now();const [jsRes,cssRes]=await Promise.all([fetch(jsUrl),fetch(cssUrl)]);if(!jsRes.ok||!cssRes.ok)throw new Error('無法從CDN下載檔案');const jsCode=await jsRes.text();const cssCode=await cssRes.text();const style=d.createElement('style');style.id='kqt-style';style.textContent=cssCode;d.head.appendChild(style);const script=d.createElement('script');script.id=id;script.textContent=jsCode;d.body.appendChild(script);}catch(e){alert('載入查詢工具時發生錯誤: '+e.message);}}) (document);
